// This file was auto-generated by Fern from our API Definition.

import * as Mesta from "../../../src/api/index";
import { MestaClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("DocumentsClient", () => {
    test("getPresignedUrl (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: { url: "url", fileName: "fileName", type: "identity", expiresIn: 300 },
            requestId: 1,
        };

        server
            .mockEndpoint()
            .get("/v1/beneficiaries/beneficiaryId/documents/documentId/presigned-url")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.beneficiaries.documents.getPresignedUrl({
            beneficiaryId: "beneficiaryId",
            documentId: "documentId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getPresignedUrl (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/beneficiaries/beneficiaryId/documents/documentId/presigned-url")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPresignedUrl({
                beneficiaryId: "beneficiaryId",
                documentId: "documentId",
            });
        }).rejects.toThrow(Mesta.BadRequestError);
    });

    test("getPresignedUrl (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/beneficiaries/beneficiaryId/documents/documentId/presigned-url")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPresignedUrl({
                beneficiaryId: "beneficiaryId",
                documentId: "documentId",
            });
        }).rejects.toThrow(Mesta.UnauthorizedError);
    });

    test("getPresignedUrl (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/beneficiaries/beneficiaryId/documents/documentId/presigned-url")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPresignedUrl({
                beneficiaryId: "beneficiaryId",
                documentId: "documentId",
            });
        }).rejects.toThrow(Mesta.ForbiddenError);
    });

    test("getPresignedUrl (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/beneficiaries/beneficiaryId/documents/documentId/presigned-url")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPresignedUrl({
                beneficiaryId: "beneficiaryId",
                documentId: "documentId",
            });
        }).rejects.toThrow(Mesta.NotFoundError);
    });

    test("getPresignedUrl (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/v1/beneficiaries/beneficiaryId/documents/documentId/presigned-url")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPresignedUrl({
                beneficiaryId: "beneficiaryId",
                documentId: "documentId",
            });
        }).rejects.toThrow(Mesta.InternalServerError);
    });

    test("getPurposeOfPaymentPresignedUrl (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { url: "url", fileName: "fileName", type: "type", blob: "blob", expiresIn: 300 };

        server
            .mockEndpoint()
            .get("/v2/beneficiaries/id/purpose-of-payment-document/presigned-url")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.beneficiaries.documents.getPurposeOfPaymentPresignedUrl({
            id: "id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getPurposeOfPaymentPresignedUrl (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/beneficiaries/id/purpose-of-payment-document/presigned-url")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPurposeOfPaymentPresignedUrl({
                id: "id",
            });
        }).rejects.toThrow(Mesta.UnauthorizedError);
    });

    test("getPurposeOfPaymentPresignedUrl (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/beneficiaries/id/purpose-of-payment-document/presigned-url")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPurposeOfPaymentPresignedUrl({
                id: "id",
            });
        }).rejects.toThrow(Mesta.ForbiddenError);
    });

    test("getPurposeOfPaymentPresignedUrl (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new MestaClient({
            maxRetries: 0,
            apiKey: "test",
            apiSecret: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/beneficiaries/id/purpose-of-payment-document/presigned-url")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.beneficiaries.documents.getPurposeOfPaymentPresignedUrl({
                id: "id",
            });
        }).rejects.toThrow(Mesta.NotFoundError);
    });
});
