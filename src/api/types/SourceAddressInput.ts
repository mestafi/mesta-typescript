
export interface SourceAddressInput {
    /** The blockchain address to be added as a source */
    address: string;
    /** The blockchain network for the address (e.g., 'ethereum', 'polygon') */
    chain: string;
}
