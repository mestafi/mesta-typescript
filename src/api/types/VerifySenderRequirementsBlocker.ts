// This file was auto-generated by Fern from our API Definition.

import type * as Mesta from "../index.js";

/**
 * An actionable public requirement that prevents sender verification. Its blocker semantics match setup-request blockers; internal canonical requirement keys are not exposed.
 */
export interface VerifySenderRequirementsBlocker {
    /** Machine-readable blocker code. */
    code: VerifySenderRequirementsBlocker.Code;
    /** Accepted capabilities that require this data or associate. */
    requiredBy?: Mesta.PublicSenderCapability[] | undefined;
    /** The sender or existing UBO missing data, or the associate role that must be created. */
    subject?: VerifySenderRequirementsBlocker.Subject | undefined;
    /** Missing public properties on the identified subject. Requirements sharing the same subject and action are grouped. */
    fields?: VerifySenderRequirementsBlocker.Fields.Item[] | undefined;
    /** The next API action for resolving this blocker. Follow the returned method, path, and accepted body fields. */
    action?: VerifySenderRequirementsBlocker.Action | undefined;
}

export namespace VerifySenderRequirementsBlocker {
    /** Machine-readable blocker code. */
    export const Code = {
        MissingData: "missing_data",
        MissingAssociate: "missing_associate",
        MestaAdminActionRequired: "mesta_admin_action_required",
    } as const;
    export type Code = (typeof Code)[keyof typeof Code];

    /**
     * The sender or existing UBO missing data, or the associate role that must be created.
     */
    export interface Subject {
        type: Subject.Type;
        /** ID of the existing sender, UBO, or associate whose data is missing. */
        id?: string | undefined;
        /** Required role or roles when `type` is `associate`. */
        roles?: string[] | undefined;
        /** Minimum number of associates with the specified role or roles. */
        minimumCount?: number | undefined;
    }

    export namespace Subject {
        export const Type = {
            Sender: "sender",
            Ubo: "ubo",
            Associate: "associate",
        } as const;
        export type Type = (typeof Type)[keyof typeof Type];
    }

    export type Fields = Fields.Item[];

    export namespace Fields {
        export interface Item {
            /** Public subject property, never an internal canonical key. */
            name?: string | undefined;
            label: string;
            /** For document fields, the exact upload `type`. */
            documentType?: Item.DocumentType | undefined;
        }

        export namespace Item {
            /** For document fields, the exact upload `type`. */
            export const DocumentType = {
                BusinessRegistrationProof: "business_registration_proof",
                AddressProof: "address_proof",
                FiRegistrationProof: "fi_registration_proof",
                DirectorsRegistry: "directors_registry",
            } as const;
            export type DocumentType = (typeof DocumentType)[keyof typeof DocumentType];
        }
    }

    /**
     * The next API action for resolving this blocker. Follow the returned method, path, and accepted body fields.
     */
    export interface Action {
        method: Action.Method;
        path: string;
        bodyFields?: string[] | undefined;
    }

    export namespace Action {
        export const Method = {
            Post: "POST",
            Patch: "PATCH",
        } as const;
        export type Method = (typeof Method)[keyof typeof Method];
    }
}
